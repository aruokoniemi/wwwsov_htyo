"use strict";
/*
var modalLogin = carGameApp.service("modalLogin", function ($scope, $modal, $rootScope) {
  return function() {
    var instance = $modal.open({
      templateUrl: '/components/login/modalLogin.html',
      controller: 'loginController',
      scope: $scope
    })
  };
});
*/